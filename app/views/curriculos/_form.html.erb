<%= form_with(model: curriculo, local: true) do |form| %>
  <% if curriculo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(curriculo.errors.count, "error") %> prohibited this curriculo from being saved:</h2>

      <ul>
      <% curriculo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome, id: :curriculo_nome %>
  </div>

  <div class="field">
    <%= form.label :curso_id %>
    <%= form.number_field :curso_id, id: :curriculo_curso_id %>
  </div>

  <div class="field">
    <%= form.label :matriz_id %>
    <%= form.number_field :matriz_id, id: :curriculo_matriz_id %>
  </div>

  <div class="field">
    <%= form.label :disciplina_id %>
    <%= form.number_field :disciplina_id, id: :curriculo_disciplina_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
