<%= form_with(model: matriz, local: true) do |form| %>
  <% if matriz.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(matriz.errors.count, "error") %> prohibited this matriz from being saved:</h2>

      <ul>
      <% matriz.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome, id: :matriz_nome %>
  </div>

  <div class="field">
    <%= form.label :curso_id %>
    <%= form.number_field :curso_id, id: :matriz_curso_id %>
  </div>

  <div class="field">
    <%= form.label :curriculo_id %>
    <%= form.number_field :curriculo_id, id: :matriz_curriculo_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
